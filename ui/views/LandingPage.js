import Layout from '../components/Layout.js';

export default function LandingPage() {
  return Layout(\`
    <h1>Welcome to mfw-starter (Express)</h1>
    <p>This is server-rendered using Express and mfw.</p>
  \`);
}
